server:
  port: 8848
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/codecwf/hrm_config.git
          username: codecwf
          password: c95w02f22
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    prefer-ip-address: true